Программа выведет: nil, false

Интерфейс в Go представляет структуру с двумя полями: динамическое значение(value) и 
динамический тип(type). Так как функция Foo возвращает иетерфейс, то значение становится nil,
а тип os.PathError. В следствии проверки на nil, происзодит сравнение является ли err 
nil-интерфейсом, т.е. интерфейсом, у которого оба поля, как value, так и type, имеют
значение nil, что приводит к false.